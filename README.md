# mini-git

**Challenges**: Allow users to create a repository, add files, do commits, view commit history, revert to older versions

**Action**: Implementation of doubly linked list in C, allocating nodes for commit, GCC Compiler, File I/O 
